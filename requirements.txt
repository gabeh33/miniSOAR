# Requirements to run the program, recommend creating a virtual environment using 
# python3 -m venv venv 
# source venv/bin/activate
# pip install -r requirements.txt
azure-identity
msgraph-sdk
requests
